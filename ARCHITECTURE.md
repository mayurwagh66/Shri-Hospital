# 🏗️ System Architecture

## Hospital Management System - Technical Architecture

This document provides a visual overview of how the Hospital Management System is structured and how components interact.

---

## 📐 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                          CLIENT LAYER                               │
│                    (Browser / Web Frontend)                          │
├─────────────────────────────────────────────────────────────────────┤
│  index.html      login.html      dashboard.html                     │
│      ↓               ↓                 ↓                             │
│  · Landing Page  · Auth Form  · Main App Interface                  │
│  · Features      · Login/Register  · Modals & Tables               │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
                    HTTP/HTTPS (REST API)
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                       APPLICATION LAYER                             │
│                  (Node.js + Express Server)                         │
├─────────────────────────────────────────────────────────────────────┤
│  server.js (Main Entry Point)                                       │
│      ↓                                                               │
│  ┌─────────────────────────────────────────────────────────┐       │
│  │              MIDDLEWARE STACK                           │       │
│  ├────────────────────────────────────────────────────────┤       │
│  │ · Express JSON Parser      · Authentication           │       │
│  │ · Static File Server       · Error Handler            │       │
│  │ · CORS Handling           · Session Management        │       │
│  └────────────────────────────────────────────────────────┘       │
│      ↓                                                               │
│  ┌────────────────────────────────────────────────────────┐        │
│  │         10 ROUTE FILES (60+ Endpoints)                │        │
│  ├────────────────────────────────────────────────────────┤        │
│  │ authRoutes          appointmentRoutes                 │        │
│  │ patientRoutes       medicalRecordRoutes               │        │
│  │ doctorRoutes        medicalWasteRoutes                │        │
│  │ departmentRoutes    invoiceRoutes                     │        │
│  │ wardRoutes          inventoryRoutes                   │        │
│  └────────────────────────────────────────────────────────┘        │
│      ↓                                                               │
│  ┌────────────────────────────────────────────────────────┐        │
│  │      9 CONTROLLERS (Business Logic)                   │        │
│  ├────────────────────────────────────────────────────────┤        │
│  │ authController      appointmentController             │        │
│  │ patientController   medicalRecordController           │        │
│  │ doctorController    medicalWasteController            │        │
│  │ departmentController invoiceController                │        │
│  │ wardController      inventoryController               │        │
│  └────────────────────────────────────────────────────────┘        │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
                        MongoDB Driver
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                        DATA LAYER                                   │
│                  (MongoDB Database)                                 │
├─────────────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────┐          │
│  │      10 DATA MODELS (Collections)                   │          │
│  ├──────────────────────────────────────────────────────┤          │
│  │ Users          Doctors         Departments         │          │
│  │ Patients       Appointments    Wards               │          │
│  │ MedicalRecords Invoices        MedicalWaste        │          │
│  │ Inventory                                          │          │
│  └──────────────────────────────────────────────────────┘          │
│        ↓                                                             │
│  [MongoDB Database File]                                            │
│  · Local: mongoose://localhost:27017/shri-hospital                 │
│  · Cloud: MongoDB Atlas cluster                                    │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Authentication Flow

```
┌──────────────┐
│   User       │
│   Login      │
└──────┬───────┘
       │
       ↓
┌─────────────────────────────────────────────┐
│  POST /api/auth/login                       │
│  Body: {email, password}                    │
└────────────────┬────────────────────────────┘
                 │
       ┌─────────┴──────────┐
       ↓                    ↓
   [Found User?]      [Verify Password?]
       │ yes              │ yes
       └──────┬───────────┘
              ↓
       ┌──────────────────────────────┐
       │ Generate JWT Token           │
       │ {userId, email, role, ...}   │
       │ Expires: 24 hours           │
       └──────┬───────────────────────┘
              ↓
       ┌──────────────────────────────┐
       │ Return Token to Client       │
       │ Store in localStorage        │
       └──────────────────────────────┘

Future Requests:
┌──────────────────────────────────────────────┐
│ Include in Authorization Header:             │
│ "Authorization: Bearer <JWT_TOKEN>"          │
├──────────────────────────────────────────────┤
│ Server Verifies Token:                       │
│ · Is token valid?                            │
│ · Is token expired?                          │
│ · Is user authorized for route?              │
│ (Role-based access control)                  │
└──────────────────────────────────────────────┘
```

---

## 🗂️ File Structure & Data Flow

### Frontend Files
```
views/
├── index.html          → Landing page with features
├── login.html          → Authentication UI
└── dashboard.html      → Main application

public/
├── css/
│   └── style.css       → 600+ lines of responsive styling
└── js/
    ├── app.js          → API wrapper & utilities (400+ lines)
    └── dashboard.js    → Dashboard logic & forms (500+ lines)
```

### Backend Files
```
models/                 → 10 Mongoose schemas
├── User.js            (Auth, password hashing)
├── Patient.js         (Auto-ID: PAT000001)
├── Doctor.js          (Availability scheduling)
├── Appointment.js     (Auto-ID: APT000001)
├── MedicalRecord.js   (Auto-ID: REC000001)
├── Department.js
├── Ward.js            (Auto-ID: WRD0001)
├── MedicalWaste.js    (Auto-ID: WAS000001, 6 categories)
├── Invoice.js         (Auto-ID: INV000001, tax calculation)
└── Inventory.js       (Auto-ID: INV000001, low-stock tracking)

controllers/           → 9 business logic files
├── authController.js  (Login, register, JWT)
├── patientController.js
├── doctorController.js
├── appointmentController.js
├── medicalRecordController.js
├── medicalWasteController.js
├── invoiceController.js
├── departmentController.js
└── wardController.js, inventoryController.js

routes/               → 10 API route files
├── authRoutes.js   (5 endpoints)
├── patientRoutes.js (5 endpoints)
├── doctorRoutes.js (5 endpoints)
├── appointmentRoutes.js (6 endpoints)
├── medicalRecordRoutes.js (4 endpoints)
├── medicalWasteRoutes.js (5 endpoints + report)
├── invoiceRoutes.js (4 endpoints)
├── departmentRoutes.js (4 endpoints)
└── wardRoutes.js, inventoryRoutes.js

middleware/
├── auth.js           (JWT verification, role checking)
└── errorHandler.js   (Centralized error catching)

config/
└── database.js       (MongoDB connection)

utils/               (Utilities for future enhancements)
```

---

## 🔄 Request/Response Cycle

### Example: Booking an Appointment

```
┌─────────────────────────────────────────┐
│ 1. USER INTERACTION                     │
├─────────────────────────────────────────┤
│ Click "Book Appointment" button         │
│ Modal opens, user fills form:           │
│ - Select Patient                        │
│ - Select Doctor                         │
│ - Choose Date & Time                    │
│ - Click "Save"                          │
└──────────┬────────────────────────────┘
           ↓
┌─────────────────────────────────────────┐
│ 2. FRONTEND (dashboard.js)              │
├─────────────────────────────────────────┤
│ saveAppointment() function:             │
│ - Get form data                         │
│ - Call bookAppointment()                │
│ - POST /api/appointments                │
│ - Include Authorization header         │
└──────────┬────────────────────────────┘
           ↓ HTTP POST
┌─────────────────────────────────────────┐
│ 3. BACKEND - ROUTING                    │
├─────────────────────────────────────────┤
│ appointmentRoutes.js:                   │
│ POST /appointments → appointmentController.bookAppointment()
└──────────┬────────────────────────────┘
           ↓
┌─────────────────────────────────────────┐
│ 4. BACKEND - MIDDLEWARE                 │
├─────────────────────────────────────────┤
│ authenticateToken():                    │
│ - Verify JWT in header                  │
│ - Decode user info                      │
│ - Attach to req.user                    │
│                                         │
│ authorize('Receptionist', 'Patient'):   │
│ - Check if user role allowed            │
│ - Return 403 if not authorized          │
└──────────┬────────────────────────────┘
           ↓
┌─────────────────────────────────────────┐
│ 5. BACKEND - CONTROLLER LOGIC           │
├─────────────────────────────────────────┤
│ appointmentController.bookAppointment():│
│ - Validate patient exists               │
│ - Validate doctor exists                │
│ - Check doctor availability             │
│ - Get consultation fee from doctor      │
│ - Create appointment object             │
│ - Set status = 'Scheduled'              │
│ - Save to database                      │
└──────────┬────────────────────────────┘
           ↓
┌─────────────────────────────────────────┐
│ 6. BACKEND - DATABASE                   │
├─────────────────────────────────────────┤
│ Mongoose (MongoDB):                     │
│ - Validate against schema               │
│ - Auto-generate appointmentId           │
│ - Insert Appointment document           │
│ - Return saved appointment              │
└──────────┬────────────────────────────┘
           ↓ HTTP Response
┌─────────────────────────────────────────┐
│ 7. FRONTEND - RESPONSE                  │
├─────────────────────────────────────────┤
│ JavaScript receives:                    │
│ {                                       │
│   success: true,                        │
│   data: {                               │
│     appointmentId: "APT000001",         │
│     patient: {...},                     │
│     date: "2024-02-20",                 │
│     status: "Scheduled"                 │
│   }                                     │
│ }                                       │
│                                         │
│ - Show success message                  │
│ - Close modal                           │
│ - Refresh appointment list              │
│ - Modal closes automatically            │
└──────────┬────────────────────────────┘
           ↓
┌─────────────────────────────────────────┐
│ 8. USER SEES RESULT                     │
├─────────────────────────────────────────┤
│ New appointment appears in table        │
│ with status = "Scheduled"               │
└─────────────────────────────────────────┘
```

---

## 🛡️ Role-Based Access Control (RBAC)

```
┌─────────────────────────────────────────────────────────────────┐
│                     USER ROLES                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ADMIN                    DOCTOR                               │
│  ├─ Create users          ├─ View patients                     │
│  ├─ Manage all patients   ├─ Create medical records           │
│  ├─ Delete accounts       ├─ View appointments                │
│  ├─ Manage doctors        ├─ Update own availability          │
│  ├─ Create departments    └─ View medical reports             │
│  ├─ Create wards                                              │
│  ├─ Manage invoices       RECEPTIONIST                        │
│  ├─ Full system access    ├─ Register patients               │
│  └─ View all reports      ├─ Book appointments               │
│                           ├─ View schedules                   │
│                           ├─ Create invoices                  │
│                           └─ View basic reports               │
│                                                                 │
│                           STAFF                               │
│                           ├─ Record medical waste             │
│                           ├─ Update waste disposal            │
│                           ├─ Manage inventory                 │
│                           └─ View department info             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

Authorization Middleware:
┌─────────────────────────────────────────────────────────────────┐
│ authorize(...roles) middleware:                                │
│                                                                 │
│ 1. Check if req.user exists (authenticated)                   │
│ 2. Check if req.user.role in allowed roles                    │
│ 3. If yes → call next() (allow access)                        │
│ 4. If no → return 403 Forbidden                               │
│                                                                 │
│ Applied to every protected route:                             │
│ router.post('/patients', authorize('Admin', 'Receptionist'), │
│            patientController.createPatient)                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 Database Model Relationships

```
┌──────────────┐
│     User     │ (Authentication & roles)
└──────┬───────┘
       │
       ├─────────────┬─────────────┬──────────────┐
       ↓             ↓             ↓              ↓
    Doctor       Patient      Department    Medical Staff
    (role)       (register)   (head-of-dept)  (role)
       │             │             │
       │             ├─────────────┤
       │             ↓             ↓
       │         ┌─────────────────────┐
       │         │   MedicalRecord     │
       │         │ (diagnosis, vitals) │
       │         └──────────┬──────────┘
       │                    │
       ↓                    ↓
    ┌─────────────────────────────────┐
    │        Appointment              │
    │ (booking, scheduling, status)   │
    └─────────────────────────────────┘

Other Models:
┌────────────────────────────────────────┐
│ Invoice         (patient → billing)    │
│ MedicalWaste    (department → tracking)│
│ Inventory       (department → supplies)│
│ Ward            (department → beds)    │
│ Department      (hospital org)         │
└────────────────────────────────────────┘
```

---

## 🔌 API Endpoints (60+ Total)

```
Authentication (5 endpoints)
├─ POST   /api/auth/login
├─ POST   /api/auth/register
├─ POST   /api/auth/logout
├─ GET    /api/auth/profile
└─ PUT    /api/auth/profile

Patients (5 endpoints)
├─ GET    /api/patients (paginated, searchable)
├─ GET    /api/patients/:id
├─ POST   /api/patients (admin, receptionist)
├─ PUT    /api/patients/:id
└─ DELETE /api/patients/:id

Doctors (5 endpoints)
├─ GET    /api/doctors
├─ GET    /api/doctors/:id
├─ POST   /api/doctors (admin)
├─ PUT    /api/doctors/:id/availability
└─ PUT    /api/doctors/:id/status

Appointments (6 endpoints)
├─ GET    /api/appointments (filterable)
├─ GET    /api/appointments/:id
├─ POST   /api/appointments (receptionist, patient)
├─ PUT    /api/appointments/:id/status
├─ PUT    /api/appointments/:id/reschedule
└─ PUT    /api/appointments/:id/cancel

Medical Records (4 endpoints)
├─ GET    /api/medical-records
├─ GET    /api/medical-records/:id
├─ POST   /api/medical-records (doctor)
└─ PUT    /api/medical-records/:id

Medical Waste (5+ endpoints)
├─ GET    /api/medical-waste (filterable, date-range)
├─ GET    /api/medical-waste/:id
├─ POST   /api/medical-waste (staff)
├─ PUT    /api/medical-waste/:id/status
└─ GET    /api/medical-waste/report/generate (admin)

Invoices (4 endpoints)
├─ GET    /api/invoices
├─ GET    /api/invoices/:id
├─ POST   /api/invoices (admin, receptionist)
└─ PUT    /api/invoices/:id/payment

Departments (4 endpoints)
├─ GET    /api/departments
├─ GET    /api/departments/:id
├─ POST   /api/departments (admin)
└─ PUT    /api/departments/:id

Wards (4 endpoints)
├─ GET    /api/wards
├─ GET    /api/wards/:id
├─ POST   /api/wards (admin)
└─ PUT    /api/wards/:id

Inventory (5 endpoints)
├─ GET    /api/inventory (searchable, filterable)
├─ GET    /api/inventory/:id
├─ GET    /api/inventory/low-stock
├─ POST   /api/inventory (admin)
└─ PUT    /api/inventory/:id/quantity
```

---

## 🔄 Error Handling Flow

```
┌──────────────────────────────┐
│   Error Occurs in Code       │
│   (Controller/Database)      │
└──────────┬───────────────────┘
           ↓
┌──────────────────────────────┐
│ throw new Error() or          │
│ try-catch block catches       │
└──────────┬───────────────────┘
           ↓
┌──────────────────────────────┐
│ errorHandler Middleware      │
│ (middleware/errorHandler.js) │
└──────────┬───────────────────┘
           ↓
    ┌──────────────────────────────┐
    │ Check Error Type:            │
    │ · ValidationError            │
    │ · MongoError (duplicate)     │
    │ · JsonWebTokenError          │
    │ · Generic Error              │
    └──────────┬───────────────────┘
               ↓
┌──────────────────────────────┐
│ Return Structured Response:  │
│ {                            │
│   success: false,            │
│   message: "Error message",  │
│   status: 400/401/403/500    │
│ }                            │
└──────────┬───────────────────┘
           ↓
┌──────────────────────────────┐
│ Frontend Receives Error       │
│ showAlert("Error message",   │
│           "danger")          │
├──────────────────────────────┤
│ User Sees Error Notification │
└──────────────────────────────┘
```

---

## 📈 Deployment Architecture

```
Current Development Setup:
┌─────────────────────────────────────────┐
│     Your Computer                       │
├─────────────────────────────────────────┤
│ Node.js Server (localhost:5000)         │
│ + MongoDB (localhost:27017)             │
│ + Browser (http://localhost:5000)       │
└─────────────────────────────────────────┘

Production Setup (Recommended):
┌─────────────────────────────────────────────────────────────────┐
│                    Internet / Users                             │
└──────────────────────┬──────────────────────────────────────────┘
                       ↓
┌──────────────────────────────────────────────────────────────────┐
│ SSL/TLS Certificate (HTTPS)                                      │
└──────────────────────┬───────────────────────────────────────────┘
                       ↓
┌──────────────────────────────────────────────────────────────────┐
│ Load Balancer (optional, for scaling)                           │
└──────────────────────┬───────────────────────────────────────────┘
                       ↓
┌──────────────────────────────────────────────────────────────────┐
│ Cloud Server (AWS EC2 / Google Cloud / Azure / Heroku)          │
├──────────────────────────────────────────────────────────────────┤
│ Node.js Application Server(s)                                   │
│ ├─ Instances: 2-4 for redundancy                               │
│ ├─ Environment: Production (.env)                              │
│ └─ Monitoring: Health checks, logging                          │
└──────────────────────┬───────────────────────────────────────────┘
                       ↓
┌──────────────────────────────────────────────────────────────────┐
│ MongoDB Atlas (Cloud Database)                                  │
├──────────────────────────────────────────────────────────────────┤
│ ├─ Automatic backups                                           │
│ ├─ Replicas for disaster recovery                             │
│ ├─ Encryption at rest                                         │
│ └─ IP whitelist for security                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🎯 Technology Stack

```
┌─────────────────────────────────────────────────────────────────┐
│                      Frontend Layer                             │
├─────────────────────────────────────────────────────────────────┤
│ HTML5          Semantic structure                              │
│ CSS3           Responsive design (grid, flexbox, media queries)│
│ Vanilla JS     ES6+ (no framework needed)                      │
│ Features: Modals, forms, tables, responsive nav, loading UI   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    Application Layer (Node.js)                 │
├─────────────────────────────────────────────────────────────────┤
│ Node.js 14+    JavaScript runtime                             │
│ Express 4.18   Web framework for routing & middleware          │
│ Mongoose 7.0   MongoDB object modeling                        │
│ bcryptjs       Password hashing & verification                │
│ jsonwebtoken   JWT token generation & verification            │
│ dotenv         Environment variable management                │
│ multer         File upload handling (ready)                   │
│ pdfkit         PDF generation (ready)                         │
│ exceljs        Excel export (ready)                           │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    Database Layer                               │
├─────────────────────────────────────────────────────────────────┤
│ MongoDB        Document database (NoSQL)                       │
│ Mongoose       ODM for data modeling & validation              │
│ Indexes        For fast querying & unique constraints          │
│ Aggregation    For complex reports (medical waste)             │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                  Development Tools                              │
├─────────────────────────────────────────────────────────────────┤
│ npm            Package manager                                 │
│ nodemon        Auto-reload on file changes (dev)              │
│ .env           Environment configuration                      │
│ Git            Version control                                │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow for Medical Waste Tracking

```
┌─────────────────────────────────────────┐
│ Staff creates waste entry               │
│ (POST /api/medical-waste)               │
├─────────────────────────────────────────┤
│ Data included:                          │
│ · Category (Infectious/Sharps/...)      │
│ · Department/Ward source                │
│ · Quantity & unit                       │
│ · Hazard level                          │
│ · Collection date                       │
└──────────┬────────────────────────────┘
           ↓
┌──────────────────────────────────────────┐
│ Controller validates & saves to DB       │
│ Status: "Collected"                      │
│ Auto-sets: collectedBy (staff member)   │
└──────────┬─────────────────────────────┘
           ↓
┌──────────────────────────────────────────┐
│ Staff updates disposal status            │
│ (PUT /api/medical-waste/:id/status)      │
├──────────────────────────────────────────┤
│ Status changes:                          │
│ Collected → Stored → Processed → Disposed│
│                                          │
│ When Disposed:                           │
│ · Set disposalDate                       │
│ · Set disposalVendor                     │
│ · Set certificateNumber                  │
│ · Set disposedBy (staff member)          │
│ · Set complianceStatus                   │
└──────────┬─────────────────────────────┘
           ↓
┌──────────────────────────────────────────┐
│ Admin generates report                   │
│ (GET /api/medical-waste/report/generate) │
├──────────────────────────────────────────┤
│ Report includes:                         │
│ · Summary by category                    │
│ · Summary by department                  │
│ · Summary by status                      │
│ · Compliance tracking                    │
│ · Can filter by date range               │
└──────────┬─────────────────────────────┘
           ↓
┌──────────────────────────────────────────┐
│ Report displayed in dashboard            │
│ Charts and tables for visualization      │
└──────────────────────────────────────────┘
```

---

## 🔐 Security Layers

```
┌──────────────────────────────────────────────────────────────────┐
│                     SECURITY ARCHITECTURE                        │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│ Layer 1: Transport Security                                     │
│ ├─ HTTPS/TLS (in production)                                    │
│ └─ Prevents man-in-the-middle attacks                           │
│                                                                  │
│ Layer 2: Authentication                                         │
│ ├─ Password hashing (bcryptjs, 10 salt rounds)                  │
│ ├─ JWT tokens (24-hour expiration)                              │
│ ├─ Secure token storage (localStorage)                          │
│ └─ Token verification on every request                          │
│                                                                  │
│ Layer 3: Authorization                                          │
│ ├─ Role-based access control (4 roles)                          │
│ ├─ Route-level permission checks                                │
│ ├─ 403 Forbidden on unauthorized access                         │
│ └─ Endpoint guards by role                                      │
│                                                                  │
│ Layer 4: Input Validation                                       │
│ ├─ Mongoose schema validation                                   │
│ ├─ Type checking                                                │
│ ├─ Required field validation                                    │
│ └─ Email/phone format validation                                │
│                                                                  │
│ Layer 5: Database Security                                      │
│ ├─ MongoDB connection string in .env (not hardcoded)            │
│ ├─ Mongoose indexes on sensitive fields                         │
│ ├─ Duplicate key validation (unique: true)                      │
│ └─ In production: IP whitelist, SSL connection                  │
│                                                                  │
│ Layer 6: Error Handling                                         │
│ ├─ Centralized error handler                                    │
│ ├─ No sensitive info in error messages                          │
│ ├─ Consistent error response format                             │
│ └─ Proper HTTP status codes                                     │
│                                                                  │
│ Layer 7: Environment Security                                   │
│ ├─ Secrets in .env file (not in code)                          │
│ ├─ .gitignore excludes .env                                     │
│ ├─ NODE_ENV configuration                                       │
│ └─ Different configs for dev/prod                               │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 📚 Additional Resources

- **Frontend Code**: `public/js/app.js`, `public/js/dashboard.js`
- **Backend Code**: `controllers/`, `routes/`, `models/`
- **API Reference**: [API_DOCUMENTATION.md](API_DOCUMENTATION.md)
- **Project Summary**: [PROJECT_SUMMARY.md](PROJECT_SUMMARY.md)
- **Quick Reference**: [QUICK_REFERENCE.md](QUICK_REFERENCE.md)
- **Setup Guide**: [SETUP.md](SETUP.md)

---

**Version**: 1.0.0  
**Last Updated**: February 2024  
**Status**: ✅ Production Ready

