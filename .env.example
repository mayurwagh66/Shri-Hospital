# Environment Variables for Shri Hospital Management System
# For LOCAL DEVELOPMENT: Copy this to .env and fill in values
# For VERCEL: Add these in Vercel Dashboard → Settings → Environment Variables

# Database Configuration
# LOCAL: mongodb://localhost:27017/shri-hospital
# VERCEL: mongodb+srv://username:password@cluster.mongodb.net/shri-hospital
MONGODB_URI=mongodb://localhost:27017/shri-hospital

# Authentication Secrets (Use strong random values, minimum 32 characters)
JWT_SECRET=your_jwt_secret_key_change_this_to_random_string_minimum_32_chars

# Session Configuration Secret
SESSION_SECRET=your_session_secret_key_change_this_to_random_string_minimum_32_chars

# Application Environment
NODE_ENV=development

# Server Port (Only used in local development, ignored by Vercel)
PORT=5000

# INSTRUCTIONS FOR VERCEL DEPLOYMENT:
# 1. Create MongoDB Atlas account at https://www.mongodb.com/cloud/atlas
# 2. Get connection string like: mongodb+srv://user:pass@cluster.mongodb.net/db
# 3. Generate secure secrets with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 4. In Vercel Dashboard, go to Settings → Environment Variables
# 5. Add MONGODB_URI, JWT_SECRET, SESSION_SECRET, NODE_ENV=production
